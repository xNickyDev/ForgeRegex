{
  "name": "forge.regex",
  "version": "1.0.1",
  "description": "A ForgeScript extension that provides support for advanced regex functions.",
  "main": "dist/index.js",
  "scripts": {
    "docgen": "tsc && typedoc src/index.ts && node dist/docgen",
    "commit": "npm run docgen && node dist/commit",
    "test": "tsc && node dist/__tests__/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tryforge/ForgeRegex.git"
  },
  "keywords": [],
  "author": "itsnicky.",
  "license": "ISC",
  "devDependencies": {
    "@tryforge/forgescript": "^2.3.0",
    "@types/node": "^22.9.0",
    "typedoc": "^0.26.11",
    "typescript": "^5.6.3"
  }
}
